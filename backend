// AgriEdge OMS Backend (Node.js - Single File)
// Run: npm init -y && npm install express cors body-parser
// Start: node server.js

const express = require('express');
const cors = require('cors');
const bodyParser = require('body-parser');

const app = express();
app.use(cors());
app.use(bodyParser.json());

let orders = [];
let inventory = [];

// Create Order
app.post('/orders', (req, res) => {
  const { customer, product, quantity } = req.body;

  if (!customer || !product || !quantity) {
    return res.status(400).json({ message: 'Missing fields' });
  }

  const order = {
    id: orders.length + 1,
    customer,
    product,
    quantity,
    status: 'New'
  };

  orders.push(order);
  res.json(order);
});

// Get Orders
app.get('/orders', (req, res) => {
  res.json(orders);
});

// Complete Order
app.put('/
